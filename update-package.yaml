---
- name: Update Oracle Linux
  hosts: all
  become: yes

  tasks:

  - name: Update repository index
    dnf:
      update_cache: yes


  - name: Updating all the packages
    ansible.builtin.dnf:
      name:
        - '*'
      state: latest
      update_only: yes
